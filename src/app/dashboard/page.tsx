"use client"

import { SideBar } from "../components/SideBar";

export default function Dashboard() {
  return (
    <div className="flex items-center lex bg-background h-screen">
      <SideBar />
    </div>
  )
}